@using ValmiStore.Model.Entities.Order
@using ViewRes
@model Webmall.UI.Models.Payments.PaymentModel

@{
    ViewBag.Title = SharedResources.PaymentAccepted;
    Layout = "~/Views/Shared/_PersonalLayout.cshtml";
}

<aside id="catalog-content">
    @{ Html.RenderPartial("OrderHeaderPartial", Model.Order); }

    <ul class="breadcrumbs" id="pageBreadscrumbs">
        <li><span id="bs1">@SharedResources.WareSelection</span></li>
        <li>&#8250;</li>
        <li><span id="bs2">@SharedResources.PaymentForm</span></li>
        <li>&#8250;</li>
        <li><span id="bs3">@SharedResources.Delivery</span></li>
        <li>&#8250;</li>
        <li><span id="bs4" class="current">@SharedResources.OrderCompleting</span></li>
    </ul>

    <div class="clear-div"> </div>
    <div class="invoice-wrapper">
        <div class="invoice">
            <div class="invoice-border">
                <a id="logo-small"><img src="@Url.Content("~/ExtContent/images/logo-invoice.png")" alt=""> </a>
                <h2>@SharedResources.OrderPayment</h2>
                <div class="clear-div"> </div>
                <div class="payment-confirmation">
                    @(new HtmlString(string.Format(SharedResources.PaymentAcceptedMessage, Model.OrderId, Model.Amount)))
                </div>
                @{ Html.RenderPartial("_SummaryOrderInfo", Model.Order); }

                <div class="clear-div"> </div>

            </div>
            <div class="right">
                <input type="button" class="button-submit" value="@SharedResources.Next" onclick="window.location = '@Url.Action("Summary", "Order", new { orderId = Model.OrderId, paymentId = (int) PaymentTypes.Visa })'">
            </div>
        </div>
        @{ Html.RenderPartial("_SummaryVidgets"); }
    </div>
</aside>

